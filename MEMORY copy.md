# Memory 功能说明

## 这是什么？

这是一个给 AI 助手加“长期记忆”的模块。

它解决的核心问题很简单：

- 普通对话系统通常只记得当前这一小段上下文
- 一旦会话结束、服务重启，很多信息就丢了
- 用户下次再来时，AI 往往又要重新认识这个人

现在这套 Memory 模块的目标，是让 AI 像一个“长期陪伴者”一样，逐步记住用户的重要信息，并且在后续对话里自然用上。

---

## 现在这套 Memory 已经能做什么？

目前已经具备 5 个关键能力：

### 1. 记住用户的长期信息

系统会把值得长期保留的信息整理成结构化记忆，而不是只存原始聊天记录。

它目前主要记 3 类内容：

- `Persona`
  用户是谁，比如姓名、职业、关系状态、所在城市、工作背景
- `Preference`
  用户偏好什么，比如喜欢什么饮品、食物、音乐、活动、社交方式、居住偏好
- `Event`
  用户经历过什么，比如搬家、离婚、工作变化、情绪事件、长期困扰

简单说：

- `Persona` 是“这个人是谁”
- `Preference` 是“这个人喜欢什么”
- `Event` 是“这个人经历过什么”

---

### 2. 支持动态更新

这套系统不是“第一次记住就永远不变”，而是允许用户后面改口。

比如：

- 之前说“我住在上海”
- 后来又说“我上个月搬到杭州了”

系统会把“当前城市”更新成杭州，而不是一直保留旧值。

再比如：

- 之前说“我最喜欢喝拿铁”
- 后来说“我现在更喜欢美式”

系统会把饮品偏好更新成新的值。

---

### 3. 支持冲突管理

当新信息和旧信息冲突时，系统不会悄悄覆盖掉，而是会记录下来。

例如：

- 旧值：`divorced`
- 新值：`remarried`

系统会：

- 把当前有效值改成 `remarried`
- 同时记一条 conflict 记录，保存“之前是什么、后来变成了什么”

这很重要，因为这样后面可以追踪：

- 用户信息是什么时候变的
- 系统为什么做了更新
- 旧值和新值分别是什么

---

### 4. 支持相关记忆召回

系统不会每次都把全部历史记忆塞给模型。

它会根据当前用户问题，优先召回“最相关”的长期记忆。

例如：

- 用户问“你记得我现在住在哪儿吗？”
  系统会优先找城市相关的记忆
- 用户问“你记得我现在喜欢喝什么吗？”
  系统会优先找饮品偏好相关的记忆
- 用户问“你知道我最近为什么心情不太好吗？”
  系统会优先找和情绪、关系、事件相关的长期记忆

这样做的好处是：

- 回答更连贯
- 回答更像“认识这个人”
- 不会把无关记忆全塞进去，避免噪音太大

---

### 5. 支持“先抽取，再写入”的两阶段机制

这是这次升级里最重要的点。

现在写记忆不是直接靠本地规则硬塞进去，而是分成两步：

#### 第一步：抽取候选记忆

系统先调用 `gpt-4o-mini`，把用户这句话整理成结构化候选记忆。

比如用户说：

> 我叫小雨，是一名插画师，住在上海。我喜欢喝拿铁。

模型会尽量提取成类似这样的结构：

- `name = 小雨`
- `occupation = 插画师`
- `home_city = 上海`
- `favorite_beverage = 拿铁`

#### 第二步：本地写入策略决策

抽取完以后，再由本地代码决定：

- 这是新增记忆
- 这是更新已有 slot
- 这是冲突，需要记录
- 这是事件记忆，应该单独存

这样做的好处是：

- 抽取更灵活，不再完全依赖死规则
- 写入更可控，不会把“要不要覆盖旧值”完全交给模型

---

## 这套系统是怎么工作的？

可以把它理解成下面这条链路：

### 第 1 步：用户说一句话

例如：

> 我现在搬到杭州了，而且最近更喜欢美式咖啡。

### 第 2 步：模型先做“记忆抽取”

系统调用 `gpt-4o-mini`，尝试识别出：

- 哪些信息值得长期保存
- 它们属于哪一类记忆
- 应该写到哪个 slot

### 第 3 步：本地策略决定怎么入库

本地代码会判断：

- 这是新信息吗？
- 这个 slot 以前有没有值？
- 如果有旧值，现在是不是发生了更新？
- 是否需要记一条 conflict？

### 第 4 步：存到本地记忆库

当前版本会把长期记忆持久化到本地 JSON 文件里。

所以即使服务重启，记忆也不会丢。

### 第 5 步：后续对话时按需召回

下次用户再提到相关话题时，系统会按 query 检索出最相关的长期记忆，再注入给模型。

---

## 和最早的“简单 memory”相比，升级了什么？

以前的简单版本更像是：

- 把最近几轮对话直接存下来
- 回答前再整体贴回去

这能解决“短期上下文”问题，但不适合做长期陪伴。

现在的版本升级成了：

- 不只是存原始聊天
- 会把记忆整理成结构化内容
- 会区分“人设 / 偏好 / 事件”
- 会做动态更新
- 会记录冲突
- 会做相关记忆召回
- 会用 LLM 先做抽取，再由本地策略决定怎么写

所以现在更接近一个真正的“长期记忆系统”，而不是单纯的聊天历史缓存。

---

## 目前采用了哪些技术思路？

从实现角度看，现在主要用了三层：

### 1. 结构化 Memory Slots

把用户长期信息放进固定槽位里，例如：

- `name`
- `occupation`
- `relationship_status`
- `home_city`
- `favorite_beverage`
- `favorite_food`
- `music_style`
- `social_style`
- `living_preference`

这样后面更新、检索、冲突管理都会更清楚。

### 2. 语义召回

除了 slot 以外，系统也会保留事件类记忆，并为记忆构造向量表示，支持“按语义找相关记忆”。

当前是一个本地可跑的轻量实现，重点是先把框架跑通。

### 3. 元信息过滤

召回时不只是看“像不像”，还会结合一些元信息，例如：

- 记忆类型
- slot 类型
- 是否是当前有效值
- 时间新鲜度

所以它不是简单全文搜索，而是“相关性 + 结构信息”一起决定。

---

## 真实测试测到了什么程度？

目前已经做过两类测试：

### 1. 模拟对话测试

用手写的模拟对话，验证：

- 跨会话记忆是否保留
- 用户改口后 slot 是否更新
- conflict 是否被记录
- 检索时是否能拿到最新值

### 2. 真实 JSON 数据测试

我们还拿了真实 profile / conversation JSON 做过小规模测试。

这一轮用了 3 个真实 profile，主要测的是：

- 用户关系状态更新
- 饮品偏好更新
- 食物偏好更新
- 音乐偏好更新
- 居住偏好更新
- 社交偏好更新

结果上，核心机制已经通了：

- 新信息可以覆盖旧信息
- 冲突可以被记录
- 最终召回基本能拿到更新后的最新值

也就是说：

这套系统已经不只是“能存记忆”，而是“能维护会变化的长期记忆”。

---

## 现在的边界和不足是什么？

虽然现在已经能用，但还不是最终形态。

目前最主要的边界有 3 个：

### 1. 抽取质量仍然受模型稳定性影响

LLM 抽取比纯规则灵活很多，但偶尔也会：

- 漏掉明显事实
- 表达不够规范
- 抽出的 value 不够统一

所以当前系统做了一个折中：

- 以 LLM 抽取为主
- 用规则做补漏

### 2. 目前是本地文件存储

现在为了快速验证，记忆是写在本地 JSON 文件里的。

这适合 demo 和单机测试，但如果后面要正式用于多用户系统，就需要升级成：

- 数据库
- 向量库
- 或者专门的 memory service

### 3. 还没有做完整的大规模评测

目前已经验证了机制是通的，但还没有做大样本统计。

后面如果要更正式评估，可以继续补：

- 更新成功率
- 冲突命中率
- 召回正确率
- 噪声记忆比例

---

## 这套 Memory 最适合什么场景？

它特别适合以下场景：

- 情感陪伴
- 长周期咨询式对话
- 需要持续了解用户偏好的 AI 助手
- 需要跨会话保持一致性的角色型 Agent

因为这些场景最怕的问题就是：

- 每次都重新认识用户
- 回答前后不一致
- 忘记用户之前说过的重要背景

而这正是长期记忆系统要解决的问题。

---

## 文件结构

当前和 Memory 直接相关的核心文件是：

- [memory/base.py](/mnt/dengzhijie/mydata/memory/pipecat/examples/quickstart/memory/base.py)
  定义统一的 memory 接口
- [memory/structured.py](/mnt/dengzhijie/mydata/memory/pipecat/examples/quickstart/memory/structured.py)
  当前长期记忆主实现
- [memory/semantic.py](/mnt/dengzhijie/mydata/memory/pipecat/examples/quickstart/memory/semantic.py)
  本地语义检索相关工具
- [memory/llm_extractor.py](/mnt/dengzhijie/mydata/memory/pipecat/examples/quickstart/memory/llm_extractor.py)
  第一阶段的 LLM 记忆抽取
- [memory_processor.py](/mnt/dengzhijie/mydata/memory/pipecat/examples/quickstart/memory_processor.py)
  把长期记忆注入到对话上下文
- [bot.py](/mnt/dengzhijie/mydata/memory/pipecat/examples/quickstart/bot.py)
  当前 quickstart 的入口

---

## 一句话总结

现在这套 Memory，不再只是“保存聊天记录”。

它已经升级成了一个面向长期陪伴场景的记忆系统，能：

- 识别值得长期保存的信息
- 结构化存储用户 persona、偏好和事件
- 在用户信息变化时动态更新
- 记录冲突
- 在后续对话中按需召回最相关的长期记忆

如果继续往下做，下一步重点就不是“有没有记忆”，而是“记忆提取得够不够准、召回得够不够稳”。


  cd /mnt/dengzhijie/mydata/memory/pipecat/examples/quickstart
  python prototype_demo/server.py

  启动后访问：

  http://127.0.0.1:7897