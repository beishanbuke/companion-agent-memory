
### 1. 记住用户的长期信息

系统会把值得长期保留的信息整理成结构化记忆

它目前主要记 3 类内容：

- `Persona`
  用户是谁，比如姓名、职业、关系状态、所在城市、工作背景
- `Preference`
  用户偏好什么，比如喜欢什么饮品、食物、音乐、活动、社交方式、居住偏好
- `Event`
  用户经历过什么，比如搬家、离婚、工作变化、情绪事件、长期困扰

---

### 2. 支持动态更新


比如：

- 之前说“我住在上海”
- 后来又说“我上个月搬到杭州了”

系统会把“当前城市”更新成杭州，而不是一直保留旧值。

再比如：

- 之前说“我最喜欢喝拿铁”
- 后来说“我现在更喜欢美式”

系统会把饮品偏好更新成新的值。

---

### 3. 支持冲突管理

例如：

- 旧值：`divorced`
- 新值：`remarried`

系统会：

- 把当前有效值改成 `remarried`
- 同时记一条 conflict 记录，保存“之前是什么、后来变成了什么”

可以追踪：

- 用户信息是什么时候变的
- 系统为什么做了更新
- 旧值和新值分别是什么

---

### 4. 支持相关记忆召回

根据当前用户问题，优先召回“最相关”的长期记忆。

例如：

- 用户问“你记得我现在住在哪儿吗？”
  系统会优先找城市相关的记忆
- 用户问“你记得我现在喜欢喝什么吗？”
  系统会优先找饮品偏好相关的记忆
- 用户问“你知道我最近为什么心情不太好吗？”
  系统会优先找和情绪、关系、事件相关的长期记忆

---

### 5. “先抽取，再写入”的两阶段机制

记忆存储总体分成两步：

#### 第一步：抽取候选记忆

系统先调用 `gpt-4o-mini`，把用户这句话整理成结构化候选记忆。

比如用户说：

> 我叫小雨，是一名插画师，住在上海。我喜欢喝拿铁。

模型会尽量提取成类似这样的结构：

- `name = 小雨`
- `occupation = 插画师`
- `home_city = 上海`
- `favorite_beverage = 拿铁`

#### 第二步：本地写入策略决策

抽取完以后，再由本地代码决定：

- 这是新增记忆
- 这是更新已有 slot
- 这是冲突，需要记录
- 这是事件记忆，应该单独存


---

## 工作Pipeline


### 第 1 步：用户Query

例如：

> 我现在搬到杭州了，而且最近更喜欢美式咖啡。

### 第 2 步：外部模型做记忆抽取

系统调用 `gpt-4o-mini`，尝试识别出：

- 哪些信息值得长期保存
- 它们属于哪一类记忆
- 应该写到哪个 slot

### 第 3 步：本地策略决定怎么入库

本地代码会判断：

- 这是新信息吗？
- 这个 slot 以前有没有值？
- 如果有旧值，现在是不是发生了更新？
- 是否需要记一条 conflict？

### 第 4 步：存到本地记忆库

当前版本会把长期记忆持久化到本地 JSON 文件里。

所以即使服务重启，记忆也不会丢。

### 第 5 步：后续对话时按需召回

下次用户再提到相关话题时，系统会按 query 检索出最相关的长期记忆，再注入给模型。

---

  cd /mnt/dengzhijie/mydata/memory/pipecat/examples/quickstart
  python prototype_demo/server.py

  启动后访问：

  http://127.0.0.1:7897